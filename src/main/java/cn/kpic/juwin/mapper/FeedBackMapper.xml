<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.kpic.juwin.mapper.FeedBackMapper">

    <insert id="save" parameterType="cn.kpic.juwin.domain.FeedBack">
        insert into tb_feed_back (url, msg, createTime)
        values (#{url}, #{msg}, #{createTime})
    </insert>

</mapper>